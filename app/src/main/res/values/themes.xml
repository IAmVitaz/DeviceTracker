<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.DeviceTracker" parent="Theme.MaterialComponents.DayNight">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/topBarColor</item>
        <item name="colorPrimaryVariant">@color/topBarColor</item>
        <item name="colorOnPrimary">@color/mainText</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/accent</item>
        <item name="colorSecondaryVariant">@color/accent</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->

        <item name="android:windowLightStatusBar">@bool/lightStatusBar</item>

        // title color
        <item name="toolbarStyle">@style/my_Toolbar</item>

        // back arrow color
        <item name="drawerArrowStyle">@style/MyDrawerArrowToggle</item>

        // search bar
        <item name="searchViewStyle">@style/MySearchViewStyle</item>

    </style>

    <style name="my_Toolbar" parent="@style/Widget.MaterialComponents.Toolbar">
        <item name="titleTextColor">@color/secondaryText</item>
    </style>

    <style name="MyDrawerArrowToggle" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="color">@color/secondaryText</item>
    </style>

    <style name="MySearchViewStyle" parent="Widget.AppCompat.SearchView">
        <!-- Background for the search query section (e.g. EditText) -->
        <item name="queryBackground">@color/secondaryText</item>
        <!-- Background for the actions section (e.g. voice, submit) -->
        <item name="submitBackground">@color/secondaryText</item>
        <!-- Close button icon -->
        <item name="closeIcon">@color/secondaryText</item>
        <!-- Search button icon -->
        <item name="searchIcon">@color/secondaryText</item>
        <!-- Go/commit button icon -->
        <item name="goIcon">@color/secondaryText</item>
        <!-- Voice search button icon -->
        <item name="voiceIcon">@color/secondaryText</item>
        <!-- Commit icon shown in the query suggestion row -->
        <item name="commitIcon">@color/secondaryText</item>
        <!-- Layout for query suggestion rows -->
        <item name="suggestionRowLayout">@color/secondaryText</item>
    </style>

</resources>
